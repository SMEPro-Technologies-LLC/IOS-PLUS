apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: moonshot-pod-kill-middleware
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - ios-plus
    labelSelectors:
      app: middleware-engine
  duration: "30s"
